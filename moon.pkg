import {
  "dowdiness/incr/types" @incr_types,
  "dowdiness/incr/internal" @internal,
  // @pipeline is imported for traits_test.mbt; the compiler reports it unused
  // because trait impls in test files do not count as package usage in MoonBit.
  "dowdiness/incr/pipeline" @pipeline,
}

// Suppress unused_package warning for @pipeline (see comment above).
options("warn-list": "-29")
